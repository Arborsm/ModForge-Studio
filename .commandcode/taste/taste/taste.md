# Taste

- User writes in Simplified Chinese; reply in Simplified Chinese while keeping code identifiers, file paths, and commands in English. Confidence: 0.95
- Feature work is driven by design docs in `docs/design/`, implemented batch by batch; keep a todo list of the remaining subtasks and continue autonomously when the user says "继续" instead of re-asking for direction. Confidence: 0.75
- The user's bar for "done" (验收) per batch: targeted unit tests green, architecture tests green, `vp run lint` with 0 errors/0 warnings, `vp run build` succeeding, and — for UI changes — the Playwright verify script passing with screenshots visually inspected. Confidence: 0.85
- This repo uses vite-plus (`vp`): `vp run lint`, `vp run build`, `vp test run --configLoader runner <paths>` (paths resolved from the repo root), and `vp run web:dev -- --host 127.0.0.1 --port 5175` for the dev server. Confidence: 0.85
- The shell environment can leak `NODE_ENV=production`, which breaks two things at once: the web dev server injects `DEV:false` (dev mocks never install, desktop-only APIs fail in the browser) and every jsdom test suite crashes at load with `No such built-in module: node:`. Prefix test and dev-server commands with `NODE_ENV=development`. Confidence: 0.9
- E2E verification runs Playwright scripts from `apps/desktop/scripts/verify/` against the local dev server; screenshots go to a temp dir and are reviewed by eye. Background shell tasks can swallow stdout — redirect output to a file and read it back instead. Confidence: 0.7
- Locale changes must stay in sync across all three files: `locales/model/**` (type definitions), `locales/dictionaries/zh-CN/**`, and `locales/dictionaries/en-US/**`. Confidence: 0.85
- New `editorState` keys added to patches must be registered in `EDITOR_ONLY_STATE_KEYS` (cp-maker draftPort) so they are stripped from the exported content.json. Confidence: 0.65
- `vp run build` can report a cache hit with exit 0 while `dist/` still holds the previous build — to prove fresh code shipped, run `vp run build --no-cache` and grep `apps/desktop/dist/assets/*.js` for a string unique to the new source (avoid dev-mock strings: dev-only code is tree-shaken out of production bundles). Confidence: 0.8
- When a UI feature depends on a desktop-command data chain, extend the dev mock (`platform/tauri/devLauncherMockCpMaker.ts`) with named fixtures keyed off the real command and payload shape instead of ad-hoc stubs, so the chain is verifiable end-to-end in the browser (e.g., `Data/Locations` + `Strings/Locations` fixtures for the localized map display-name chain). Confidence: 0.7
- New async code uses the shared task-runtime hooks (`useKeyedResourceTask`) instead of hand-rolled `cancelled` flags, and must keep the `asyncOwnership` architecture test's whitelist matching. Confidence: 0.75
- Playwright verify scripts must handle known UI hazards: error-level toasts never auto-dismiss and their hover region intercepts clicks near the dialog footer (`.notification-toast-close` is `pointer-events:none` until the toast is hovered — hover, then click; the guide-tour scrim mounts late, so re-check it inside the dismiss loop); blur the active element after closing a dialog before measuring hover-revealed controls; wait out fade transitions with `waitForFunction` on computed styles instead of fixed sleeps. Confidence: 0.75
- A stale node process squatting the dev port (5175) serves stale dep-optimizer hashes (504 "Outdated Optimize Dep" / old browserHash even after wiping the vite cache) — find it with `netstat -ano | findstr :5175` and kill it with `taskkill /F /T /PID` rather than clearing caches. Confidence: 0.75
- Stay inside the current batch's scope: fix incidental bugs that block verification, but report unrelated repo landmines (e.g., `DevMapAuthoringCompare.tsx` importing prototype HTML that was never committed — only fails under `NODE_ENV=development`) in the summary instead of fixing them unprompted. Confidence: 0.7
- Before building any custom UI control, search for an existing shared one and reuse it — the user flags reinvented controls ("这个条件项目不是有现成的共享控件了吗"). E.g., `WhenConditionEditor` is the shared condition editor for CP `When` fields; `entities/event`'s `EventConditionBuilderModal` is event-domain only and must not be repurposed there. Confidence: 0.8
- UI polish standard (from `docs/design/workbench-design-principles.md`, validated by the user's screenshot feedback): prefer the app's existing control idioms (e.g., `role="switch"` track/thumb toggles as in settings windows) over native form controls (bare checkbox/raw input), and strip redundant repeated data from tables/list rows. The user reviews finished UI visually and will demand a fix pass when it looks off. Confidence: 0.7
