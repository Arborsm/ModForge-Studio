import type { WorldAtlasViewId } from '../../../model/workbench'

const worldatlas: Record<WorldAtlasViewId, string> = {
  main: 'Main World',
  remote: 'Remote Regions',
}

export default worldatlas
